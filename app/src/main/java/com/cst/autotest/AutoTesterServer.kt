package com.cst.autotest

/**
 * Lightweight marker created when the Ktor application module is initialized.
 */
class AutoTesterServer {
    init {
        println("==== AutoTesterServer ====")
    }
}
